# StreamLSH-RAG // Locality-Sensitive Hash Routing Pipeline

A streaming classification engine mapping multi-dimensional data models into proximity indexes via sign projections in C to optimize RAG search latency.

## Initialization
```bash
python lsh_router_app.py
